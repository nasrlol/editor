- make initialization with permanentarena homogenous
		? Poisoning should take care of everything, do the rest manually,  since we are using arena's it's not that much work

- When selecting to the side of the text do select as well.
- Virtual whitespace

- Have text be clipped at pixel level and not rectangle level

- Eat up input events, because resizing a pane shouldn't be clicking buttons or selecting text.
  + concept of "selected" to receive key inputs.

- Marker in arena's to have 0 not be happening.

other:
  - Improve text rendering
    - Check text rendering extensively (xoff & leftsidebearing)
  - Unicode
    - kb_text_shape()
    - harfbuzz
    - implement all movements taking unicode into account
  - Scrolling text
  x Movements
  x Deletions
  x Selections
  - Clipboard
    x linux
    - win32
  - Undo/Redo
  - Non contiguous selection (e.g., with control)

- https://www.dgtlgrove.com/p/ui-bonus-1-simple-single-line-text
- https://rxi.github.io/textbox_behaviour.html

- popup, lister and command palette

app:
- bug when adding the same box twice.
- panels resizing

misc:
- unified logs
		- with date, system, level and stacktraces

platform:
- fix keyboard input (pressing & holding, vs text)
  - easier query of keys & modifiers.
- linux: investigate startup time.
- window resizing
- sleep when idle
- custom borders and titlebar

- Cmdline hash table

- UI Input
  - UI events as a queue so you can eat them
- ? Use signal/comm to have all the events with flags
  since the common case is no signal, it should be optional?

- Implement properties from https://www.dgtlgrove.com/p/ui-part-3-the-widget-building-language
- inset elements
- panels
  - rotate columns
  - selected panel so that input events only go to one panel
  - close panel button
  - better positioned resize border
- tabs
- windows

- tick() function?

- albedo sample 

- Smooth smooth animations
  - self-correcting exponential animation curves for hot_t & active_t

- win32: repaint and resizing.
  - 24:36

- Shader:
  - group draw calls with those that need the same texture and those that don't without changing draw order (because foreground&background) 
  - find a way to push a texture for a number of rectangles
  - then pop when not adding characters anymore

# Archived
x Ryan's layout algorithm
x Specify axis
x Draw background + border + gradient
x ui_comm from UI_AddBox()
x draw text from same shader
x center text vertically & horizontally
x specify vertical & horizontal padding for text, or maybe don't
x Assert that rect instances does not exceed the max instances allowed.\
x Use DrawRect() in DrawRectChar()
x Remove ReadOnlyMemoryBlock
x Stacks for selecting options like ryan
x make hot reloading shader optional
x Spacer
x Proper hashing of string and splitting up into DisplayString
x Solving violations
x win32: flickering
x panels: 
  x draggable array to change PercentOfParent
  x create/split new panel
  x close panel button
x proper mouse dragging
x clipping
x after splitting multiply all old panes on same axis by (1 - 1/n) and give the new pane size 1/n
x Evict boxes from cache when they aren't touched anymore
x prune stale elements from cache on the end of UI_ResolveLayout()
x Add back text rendering of the input buffer
x profile compile times on linux
  - standard libraries (linux et al): 100ms, math.h: 100ms, gl_core: 50ms
x Fix UI layouting not working for nested elements
x Metaprogramming:
  x flag enums
  x shaders
platform:
  x loop editing
  x profiler: toggle key bind
  x save/load state to disk
  windows:
    x editor_libs.o in .bat script
    x rename to editor.exe
app:
 x ui bug when using PercentOfParent, can overflow with rounding error. (tested with .25f)
   -> wasn't a rounding error.
x Custom draw command for rendering text edit view.
x replace X macros
