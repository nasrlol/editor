2. ui events as a queue so you can eat them

- clipping

- after splitting multiply all old panes on same axis by (1 - 1/n) and give the new pane size
1/n

- Clip all elements to window border
- https://www.dgtlgrove.com/p/ui-part-3-the-widget-building-language
  - all properties from here
- inset elements
- panels
  - rotate columns
  - selected panel so that input events only go to one panel
- prune stale elements from cache on the end of UI_ResolveLayout()

- ? Use signal/comm to have all the events with flags
  since the common case is no signal, it should be optional?
- open windows & close windows
  - inspecting ui window
- tick() function?

- albedo sample 

- Evict boxes from cache when they aren't touched anymore
- Smooth smooth animations
  - self-correcting exponential animation curves for hot_t & active_t
- profile compile times on linux
  - standard libraries (linux et al): 100ms, math.h: 100ms, gl_core: 50ms
- Add back text rendering of the input buffer
- Improve text rendering
  - Check text rendering extensively (xoff & leftsidebearing)
  - kb_text_shape()

- win32: repaint and resizing.
 
- 24:36

- Assertion that the text fits into the rectangle
		- otherwise clip?

- Shader:
  - group draw calls with those that need the same texture and those that don't without changing draw order (because foreground&background) 
  - find a way to push a texture for a number of rectangles
  - then pop when not adding characters anymore

- UI: Specify to attach to parent or sibling? AppendToParent


# dump 
- check out the violation calc code
- then check out the positions calc code
- we might want to decouple calculating positions and drawing the boxes for panels


# Archived
x Ryan's layout algorithm
x Specify axis
x Draw background + border + gradient
x ui_comm from UI_AddBox()
x draw text from same shader
x center text vertically & horizontally
x specify vertical & horizontal padding for text, or maybe don't
x Assert that rect instances does not exceed the max instances allowed.\
x Use DrawRect() in DrawRectChar()
x Remove ReadOnlyMemoryBlock
x Stacks for selecting options like ryan
x make hot reloading shader optional
x Spacer
x Proper hashing of string and splitting up into DisplayString
x Solving violations
x win32: flickering
x panels: 
  x draggable array to change PercentOfParent
  x create/split new panel
  x close panel button
x proper mouse dragging